# 使用说明：准备两项服务，在设置里填好即可

你自己的 Mac 运行听会台，会议和待办独立保存，不关联 Aaron 的电脑或会议档案。**必须准备：一个可用的火山语音应用；至于帮你总结的 AI，如果这台 Mac 上已经装了 Codex 或 Claude Code，设置页一键就能用，不用再申请任何 Key。**不需要租服务器、显卡或提前整理词表。

## 0. 先看这台 Mac 上有没有现成的 AI

打开设置页，第 2 步「谁来帮你总结」会自己检测：

- 装了 **Codex**（ChatGPT 桌面版自带）→ 点「用 Codex」，用的是你 ChatGPT 账号的额度，不额外花钱。
- 装了 **Claude Code** → 点「用 Claude Code」，用的是你的 Claude 订阅。
- 两个都没有 → 往下看第 2 节的 DeepSeek，五分钟弄完。

点按钮后它会真的跑一次，跑通才算数，页面会告诉你结果。要是提示「还没登录」，先把那个 App 打开登录一次再回来点。

## 1. 开通语音转写

打开 [火山语音控制台](https://console.volcengine.com/speech/app)，登录并创建或选择获授权的语音应用，开通**大模型流式语音识别**。产品入口是 [火山语音识别](https://www.volcengine.com/product/asr)。

准备语音控制台提供的 **App Key / APP ID、Access Key / Access Token、对应 Resource ID**，字段称呼以当前控制台为准。默认 Resource ID 为 `volc.seedasr.sauc.duration`，必须与实际开通的服务一致。这里用的是语音应用凭证，**不是火山方舟模型 Key，也不是通用云账号 AK/SK**。

**火山官方列出流式语音识别 20 小时免费试用额度**，见[官方额度说明](https://www.volcengine.com/product/ark)。领取资格、有效期和适用服务以你的控制台为准，不是每月固定赠送；用完后按供应商计费。

如果控制台要求实名、开通服务或充值，需要你本人完成。确认有有效试用额度或余额；具体价格、适用区域和账号资格看自己的控制台。不要把登录密码交给 AI。

如果 Aaron 后续提供单独给你用的语音应用凭证，你可以不自己购买。现在这个安装包**不带 Aaron 的现有密钥**；共享账户的费用、并发、撤销方式和管理员可见范围尚未核实，不能承诺共享账户完全看不到语音数据。

## 2. 准备翻译、总结和初步核查用的模型

最直接是打开 [DeepSeek API Keys](https://platform.deepseek.com/api_keys)，创建 API Key，并确认 API 余额或可用额度。[官方接入文档](https://api-docs.deepseek.com/)有当前模型列表和调用方式。

默认兼容原听会台配置：地址 `https://api.deepseek.com`、模型 `deepseek-chat`。如果账户不支持该模型，在“使用自己的其他模型 / 高级设置”里填写控制台当前可调用的模型名，再点“保存并测试模型”。不会自动购买或升级。

也可以用公司已授权的 OpenAI 兼容文本接口：请管理员提供 **API Key、接口地址、模型名称**，确认你的网络能访问、账户有额度、允许处理这类会议资料。三个字段缺一不可。原生 Anthropic 接口不是 OpenAI 格式，首版不能只换成 Claude Key 就运行。

**聊天订阅和 API 分开。**Claude Pro / Max 等聊天会员不能直接充当本程序的 API 额度，见 [Claude 官方说明](https://support.claude.com/en/articles/9876003-i-have-a-paid-claude-subscription-pro-max-team-or-enterprise-plans-why-do-i-have-to-pay-separately-to-use-the-claude-api-and-console)。DeepSeek 网页聊天能用，也不代表 API 已开通。软件本身不收取费用，语音和模型调用由对应供应商计费。

## 3. 安装只做一次

1. 收到净化 ZIP 后解压，保留完整文件夹。ZIP 不需要 GitHub 账号；从私有仓库直接下载则需要接受仓库邀请。
2. 双击 **安装.command**。缺 Node 会自动从官网下载安装；缺 Python 会打开 [Python 官方 Mac 下载页](https://www.python.org/downloads/macos/)，安装 Python 3.9+ 后再次打开安装文件。首次下载需要网络。
3. 首次安装自动打开填写页；以后点击听会台右上角 ⚙︎，会直接弹出同一个填写页面。热词、输入设备等在“更多 → 会议偏好”中调整。在填写页，填前面两项服务，点击“保存并测试模型”：会先保存当前输入，再发一个短请求检查模型连通。密钥不会回显，不要截包含密钥的屏幕给别人。
4. 进入听会台，允许浏览器使用麦克风。以后只需双击 **启动.command**。

**如果双击打不开：**打开 Mac 的“终端”，输入 `bash `（后面留一个空格），把解压文件夹中的 `安装.command` 拖进终端，按回车。以后启动文件也可同样用 `bash ` 加拖入 `启动.command` 运行。也可以让你的本机 AI 按这一步执行。不要关闭 Gatekeeper 或更改系统安全设置。本次已验证的是这条 bash 安装路径；你的 Mac 首次下载后的双击行为仍需实际确认。

可直接把完整项目和 [给 AI 的安装说明](../AI-SETUP.md)交给 Claude Code / Codex 等能操作本机的 AI。普通网页聊天不能代你安装本机依赖。账号登录、购买、Mac 权限弹窗仍由你完成。

## 4. 正式开会前试 30 秒

**面对面：**选“面对面开会”，说几句包含人名和产品名的话，确认出现字幕。点错字修改，再说一次，检查错词表是否生效。

**线上网页会议：**用 Chrome 打开会议网页，选“线上会 · 抓标签页声音”，在分享窗口选会议标签页并勾选分享音频。分别确认自己的声音和对方声音都有字幕。找不到音频开关或没有对方音轨时，先不要开始正式会议。

切换中文 / EN，观察字幕下的异步译文、要点与初步核查。它们会有模型处理延迟，初步核查不是联网事实证明。

点“结束并总结”后，到底部 **会议档案** 打开本场全文与总结，下载一份核对。总结失败可以重试；原文和录音会保留。再关掉网页、重新启动，检查这场会议仍在。

**正式会议期间保持 Mac 开机、联网且不休眠。**这份首版不是手机独立 App，不能靠 iPhone 在 Mac 关机后继续录音。

## 找不到记录或连不上时

- 全文与纪要：听会台底部“会议档案”；原始录音在本机数据目录的 `audio` 内。
- 运行 **自检.command**：只检查环境和配置是否存在，不打印密钥、不调用收费 API。
- 模型失败：核对余额、模型名、地址及网络；不会因为失败删除原文。
- 未听见对方：核对分享音频，而不是只换 ASR。桌面会议软件的系统音频路径需要另行配置与验收。
- 飞书自动归档是可选设置。先用本机档案，不必为了开第一场会完成飞书授权。
