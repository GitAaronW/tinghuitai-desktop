# Meeting LiveMate 0.3.2

## 装一次，以后自己更新

打开程序几秒后它会去看有没有新版。有的话右下角出现一条提示，写着版本号和这次改了什么。
点「更新」它就自己下载、校验、替换程序，完事提示你重开一次；点「×」就跳过这一版，不再打扰。

更新只动程序本身：你填的密钥、会议记录、录音一律原样保留。录音过程中不会更新。

## 不用申请 API Key 也能用

设置页第 2 步「谁来帮你总结」会自动看这台 Mac 上有没有装 **Codex**（ChatGPT 桌面版自带）或 **Claude Code**。
装了就点一下按钮，它当场试跑一次，通了就直接用你已有的额度，不用另外注册、不用充值。

没装也没关系，第 ② 条把 DeepSeek 的注册、充值、拿 Key 三步拆开写清楚了，每步都有直达链接，五分钟能弄完。
公司自建网关或别的服务商填同样三个框。

语音转写这项仍需自己开通火山，链接就在设置页输入框旁边。

## 会中

- **要点自动分组**：每隔几句把要点归成几个大标题，编号 1a / 1b / 1c，标题写的是结论不是话题名，连着读就是这场会的梗概。原来的卡片和点击改删都没变。
- **补充材料**（原「我的笔记」）：文字和图片在同一个输入框里。截图可以直接 ⌘V 粘进来，也能拖文件。点「发送给助手看」，助手会真的打开每张图看一遍，给出描述和一句整体判断，随会议一起归档。
- **说什么语言自己认**：默认「自动识别」。中文看字符，其他语言看高频词，判出来会告诉你。判错了在设置里手动定住即可。
- **新增印尼语、巴西葡语、西班牙语**：这三种在选单里单列一组，标着「会中字幕仅供参考」——实测火山对它们还听不准，所以录音会在会后用本机模型重新转写一遍，那一遍是准的。
- **按钮再按一次就关**：补充材料、会议档案、录音管理、本场纪要、更多，都不用再去点右上角的叉。
- **要点不再被自动刷新拖到底**：你翻到哪就停在哪，只有换一场会议才回到最新。
- **底栏多了「继续上一场」**：录音中途关了页面，提示条划掉也能从这里回来接着录。

## 会后

- **录音管理看板**（原「历史场次」）：一场一张卡，按周分组，能按来源和状态筛。按钮是打开、重新整理、删除。
- **删除进回收区**：只删这台电脑上的记录，30 天内可以从「最近删除」里恢复。
- **回看页重排**：跟会中界面同一套从左到右的排版——逐字稿、要点、待核查；下面一排是本场总结、待办确认、问助手。
- **待办浓缩**：一场会几十上百条要点，压成最多 5 条「我要去干嘛」，每条带一句会议依据。

## 换了新图标

浅底点阵声波，顶上一颗红点。原来那个黄底麦克风换掉了。

## 修的问题

这几版累计修了 20 多个问题，包括删除接口的编号校验、恢复失败后重启会重复删除、会中分组失败会连环调用模型、下载的文件被 macOS 拦住打不开等。
